name: conai
services:
  api:
    build:
      context: ./backend
      target: production
    restart: unless-stopped
    env_file: ./backend/.env
    networks:
      - conai
  frontend:
    build:
      context: ./frontend
      target: production
    restart: unless-stopped
    env_file: ./frontend/.env
    networks:
      - conai
    depends_on:
      - api
  caddy:
    image: caddy:2-alpine
    ports:
      - 80:80
      - 443:443
    volumes:
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - conai
    depends_on:
      - api
      - frontend

volumes:
  caddy_data:
  caddy_config:

networks:
  conai:
    driver: bridge
